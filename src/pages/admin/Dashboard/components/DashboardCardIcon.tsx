import React from "react";
import { DashboardCardProps } from "./Card";
import Icon from "@/app/components/icon/Icon";

const DashboardCardIcon = ({ type }: { type: string }) => {
   console.log(type);

   return <div className="h-10 w-10 rounded-full bg-white">
    
   </div>;
};

export default DashboardCardIcon;
